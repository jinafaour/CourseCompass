import streamlit as st

# --- CONCEPT DATABASE (Example Structure) ---
curriculum = {
    "Math 8": {
        "Pythagorean Theorem": {
            "hook": "Why it matters: Structural engineering for bridges and buildings.",
            "tactic_PA": "Use a digital or physical 3D model to calculate the hypotenuse.",
            "practice": "Calculate the diagonal of a 3m x 4m room."
        }
    },
    "Science 8": {
        "Plate Tectonics": {
            "hook": "Why it matters: Predicting earthquake zones in BC.",
            "tactic_PA": "Create a model of plate movement using clay or cardboard.",
            "practice": "Identify which boundary type creates a trench."
        }
    }
}

# --- UI LOGIC ---
st.title("🧠 BC Curriculum Practice Lab")
grade = st.selectbox("Select Grade", ["Grade 8", "Grade 9"])
subject = st.selectbox("Subject", list(curriculum.keys()))
topic = st.selectbox("Topic", list(curriculum[subject].keys()))

if st.button("Start Concept Sprint"):
    data = curriculum[subject][topic]
    st.info(data['hook'])
    st.write(f"### Strategy: {data['tactic_PA']}")
    st.warning(f"### Sprint Task: {data['practice']}")
    
    if st.button("Show Logic & Reveal"):
        st.success("Logic: The hypotenuse is the square root of the sum of squares...")
